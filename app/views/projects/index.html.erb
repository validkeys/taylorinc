<h1>Projects</h1>
<ul>
  <% for project in @projects -%>
    <li><%= link_to "#{h(project.client)} #{h(project.title)}", object_path(project), :title => "#{h(project.client)} #{h(project.title)}" %></li>
  <% end -%>
</ul>

