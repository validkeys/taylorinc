<h3>New <%= controller.controller_name.humanize.singularize %></h3>

<% form_for(@object, :url => collection_path, :html => { :multipart => true }) do |f| %>
  <%= render :partial => "form", :locals => { :f => f } %>
<% end %>

<%= link_to 'Back', collection_path %>